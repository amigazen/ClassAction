#ifndef CA_FLIST_H
#define CA_FLIST_H
/****************************************

        Class Action Source

        © 2002 by Martin R. Elsner & Salim Gasmi

        File: caflist.h

****************************************/

#include "catypes.h"
/*
class FileList{
public:
 FileList(int);         // inits list of selected files in given list (0/1)
 ~FileList();           // gives control to
 char *NextFile( TLBNode** );   // gives the name of the next file
                                        // and a pointer to the node
 char *NextSFile( TLBNode** );  // gives the name of the next selected file
                                        // and a pointer to the node
 int Selected;          // number of selected files
protected:
 TLBNode        *CurrentFile;   // the current file
 List           *List;          // the current list
 int            Number;         // number of list (0/1)
};
*/
#endif

